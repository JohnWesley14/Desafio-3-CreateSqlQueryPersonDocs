Utilizei os conhecimentos prévios de SQL, tive dificuldades apenas na parte de  `foreign key` por conta da sintaxe e no uso do  `join`, por nunca ter utilizado antes. Percebi que o path do arquivo não mostrava o `/` no select, então usei `//`.
Tive que relembrar a sintaxe em alguns momentos então fiz pesquisas no Google, StackOverflow e W3schools.
A parte mais trabalhosa foram os nomes, os CPF foram aleatórios, então obviamente não atenderiam os critérios para serem CPFs válidos. 
